// Run the Docker images from conf/containers.config with Apptainer, which converts each
// to a SIF once and caches it. Use on SLURM, where Docker is usually unavailable.
apptainer {
    enabled = true
    autoMounts = true
}
