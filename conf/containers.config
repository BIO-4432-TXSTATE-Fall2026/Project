// Images built from containers/<name>/Dockerfile and published to GHCR by
// .github/workflows/containers.yml. Ignored unless a container profile is enabled.
process {
    withName: 'HELLO' {
        container = 'ghcr.io/bio-4432-txstate-fall2026/project/template:latest'
    }
}
